#include <stdio.h>
#include <string.h>
#include <stdlib.h>

/**
 * _strlen - returns length of a string
 *
 * @s: string evaluated
 *
 * Return: Integer number length
 *
 */

int _strlen(char *s)
{
	return (strlen(s));
}
