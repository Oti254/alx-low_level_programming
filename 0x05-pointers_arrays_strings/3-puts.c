#include <stdio.h>
#include <stdlib.h>

/**
 * _puts - prints string followed by new line to stdout
 *
 * @str: string to be printed
 *
 * Return: Nothing
 *
 */
void _puts(char *str)
{
	puts(str);
}
